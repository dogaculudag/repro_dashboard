# Test database - isolated from development
# Use a separate database (e.g. different port or different DB name)
DATABASE_URL="postgresql://postgres:postgres@localhost:5435/repro_test"

# Ensured by Vitest setup
# NODE_ENV=test
